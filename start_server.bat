@echo off
rem ============================================================
rem   Legacy launcher - kept for compatibility.
rem   This simply forwards to START_APP.bat which shows the
rem   PERMANENT links (localhost + WiFi network) and opens
rem   your browser automatically.
rem ============================================================
call "%~dp0START_APP.bat"